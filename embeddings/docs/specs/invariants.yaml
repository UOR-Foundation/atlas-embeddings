# Machine-readable invariants (skeleton)
# Replace 'TODO' with actual expressions and bind to proofs and tests.
invariants:
  - id: boundary.size
    statement: "boundary_size == 48"
    status: frozen
    test_bindings: [t.boundary.size]
  - id: schedule.order
    statement: "schedule_order == 768"
    status: frozen
    test_bindings: [t.schedule.order]
  - id: response.cardinality
    statement: "|R(B)| == 96"
    status: frozen
    test_bindings: [t.r96.cardinality]
  - id: signature.range
    statement: "signature_support == 12288"
    status: frozen
    test_bindings: [t.signature.support]
  - id: rl.semiring
    statement: "B96 forms a semiring with oplus/otimes"
    status: draft
    test_bindings: [t.rl.semiring_laws]
