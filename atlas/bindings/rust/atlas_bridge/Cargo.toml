[package]
name = "atlas_bridge"
version = "0.5.0"
edition = "2021"
authors = ["Atlas Bridge Team"]
description = "Rust bindings for Atlas Bridge Context API v0.5"
license = "MIT"

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1.0"

[lib]
name = "atlas_bridge"
path = "src/lib.rs"
